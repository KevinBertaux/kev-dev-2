import '../scss/reset.scss'
import '../scss/helpers.scss'
import '../scss/style.scss'

const welcomeMessage = "✅ It's working! Ready to code 💻";
console.log(`${welcomeMessage}`);

export function add(a: number, b: number) {
    return a + b;
}
